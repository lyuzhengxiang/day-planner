# Day Planner Notes

Project instructions and the current project brief live in `AGENTS.md`.

Use that file for:

- project scope
- routes and API overview
- data model summary
- environment variables
- database path conventions
- UI and implementation guidance

## Models

- `gpt-5.4` is a real OpenAI model used throughout this repo (goal flow, plan generation, reflection). Do NOT assume it is a placeholder or "rename" it to an older model based on training-data assumptions. If you don't recognize a model name, websearch before changing it.
